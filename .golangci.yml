version: "2"
run:
  timeout: 5m
linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
  exclusions:
    paths:
      - ai-docs
issues:
  exclude-rules:
    - path: api/v1alpha1/groupversion_info.go
      linters:
        - staticcheck
      text: "SA1019"
