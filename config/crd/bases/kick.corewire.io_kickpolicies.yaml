---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: kickpolicies.kick.corewire.io
spec:
  group: kick.corewire.io
  names:
    kind: KickPolicy
    listKind: KickPolicyList
    plural: kickpolicies
    shortNames:
    - kickpolicy
    singular: kickpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.matchedWorkloads
      name: Matched
      type: integer
    - jsonPath: .status.blockedWorkloads
      name: Blocked
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KickPolicySpec configures workload scope and kick behavior.
            properties:
              discovery:
                description: |-
                  KickPolicyDiscoverySpec controls which workloads a policy manages and which of
                  their dependency changes trigger a restart. Both selectors are optional; an
                  empty or omitted selector matches everything on its axis.
                properties:
                  dependencySelector:
                    description: |-
                      DependencySelector limits which consumed Secret/ConfigMap changes trigger a
                      restart. Omit to treat every discovered dependency as a trigger.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  workloadSelector:
                    description: |-
                      WorkloadSelector limits which workloads this policy manages (the actors that
                      may be restarted). Omit to manage all supported workloads in the namespace.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              gitOps:
                description: KickPolicyGitOpsSpec configures provider gate behavior.
                properties:
                  provider:
                    default: None
                    description: |-
                      Provider selects the GitOps gate. "None" (the default) restarts without
                      consulting any GitOps tool, gated only by any native schedule windows.
                    enum:
                    - None
                    - Auto
                    - ArgoCD
                    - Flux
                    type: string
                  requireReconciled:
                    default: true
                    type: boolean
                  schedule:
                    description: KickPolicyScheduleSpec configures schedule source
                      behavior.
                    properties:
                      source:
                        default: Provider
                        description: KickPolicyScheduleSource configures schedule/window
                          source behavior.
                        enum:
                        - Provider
                        - None
                        type: string
                      windows:
                        description: Windows are KICK-native execution windows evaluated
                          without a GitOps provider.
                        items:
                          description: KickPolicyWindow is a KICK-native execution
                            window evaluated without a GitOps provider.
                          properties:
                            duration:
                              description: Duration is how long the window stays open
                                from each start (e.g. "1h").
                              pattern: ^([0-9]+(ns|us|µs|ms|s|m|h))+$
                              type: string
                            kind:
                              description: KickPolicyWindowKind selects allow or deny
                                semantics for a native window.
                              enum:
                              - Allow
                              - Deny
                              type: string
                            schedule:
                              description: Schedule is a standard 5-field cron expression
                                marking each window start.
                              type: string
                            timeZone:
                              description: TimeZone is the IANA zone used to evaluate
                                the schedule (default UTC).
                              type: string
                          required:
                          - duration
                          - kind
                          - schedule
                          type: object
                        type: array
                    type: object
                type: object
              minInterval:
                default: 30s
                pattern: ^$|^([0-9]+(ns|us|µs|ms|s|m|h))+$
                type: string
            required:
            - discovery
            type: object
          status:
            description: KickPolicyStatus reports policy matching and readiness state.
            properties:
              blockedWorkloads:
                format: int32
                type: integer
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              matchedWorkloads:
                format: int32
                type: integer
              observedGeneration:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
