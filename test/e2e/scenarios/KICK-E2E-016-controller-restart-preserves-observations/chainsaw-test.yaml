apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: controller-restart-preserves-observations
spec:
  steps:
    - name: scaffold
      try:
        - script:
            content: |
              echo "TODO: implement KICK-E2E-016 - controller restart preserves observations"
