apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-071
  namespace: kick-e2e-071
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-071
  template:
    metadata:
      labels:
        app: app-071
    spec:
      containers:
        - name: app
          image: registry.k8s.io/pause:3.10
          envFrom:
            - secretRef:
                name: app-secret
