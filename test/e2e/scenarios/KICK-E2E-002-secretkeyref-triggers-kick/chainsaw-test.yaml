apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: secretkeyref-triggers-kick
spec:
  steps:
    - name: scaffold
      try:
        - script:
            content: |
              echo "TODO: implement KICK-E2E-002 - secretKeyRef triggers kick"
