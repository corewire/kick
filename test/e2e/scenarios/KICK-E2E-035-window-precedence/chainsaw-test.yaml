apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: window-precedence
spec:
  steps:
    - name: scaffold
      try:
        - script:
            content: |
              echo "TODO: implement KICK-E2E-035 - window precedence"
