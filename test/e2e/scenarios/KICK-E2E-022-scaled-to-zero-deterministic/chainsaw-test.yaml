apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: scaled-to-zero-deterministic
spec:
  steps:
    - name: scaffold
      try:
        - script:
            content: |
              echo "TODO: implement KICK-E2E-022 - scaled-to-zero deterministic"
