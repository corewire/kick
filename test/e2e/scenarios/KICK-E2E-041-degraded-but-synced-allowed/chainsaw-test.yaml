apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: degraded-but-synced-allowed
spec:
  steps:
    - name: scaffold
      try:
        - script:
            content: |
              echo "TODO: implement KICK-E2E-041 - degraded but synced allowed"
