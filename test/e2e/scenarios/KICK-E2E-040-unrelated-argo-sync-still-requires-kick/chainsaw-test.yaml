apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: unrelated-argo-sync-still-requires-kick
spec:
  steps:
    - name: scaffold
      try:
        - script:
            content: |
              echo "TODO: implement KICK-E2E-040 - unrelated Argo sync still requires kick"
