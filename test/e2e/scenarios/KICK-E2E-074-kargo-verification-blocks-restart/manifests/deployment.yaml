apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-074
  namespace: kick-e2e-074
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-074
  template:
    metadata:
      labels:
        app: app-074
    spec:
      containers:
        - name: app
          image: registry.k8s.io/pause:3.10
          envFrom:
            - secretRef:
                name: app-secret
