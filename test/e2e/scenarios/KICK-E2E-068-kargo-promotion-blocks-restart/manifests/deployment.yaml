apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-068
  namespace: kick-e2e-068
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-068
  template:
    metadata:
      labels:
        app: app-068
    spec:
      containers:
        - name: app
          image: registry.k8s.io/pause:3.10
          envFrom:
            - secretRef:
                name: app-secret
