apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: optional-missing-secret-creation-triggers-kick
spec:
  steps:
    - name: scaffold
      try:
        - script:
            content: |
              echo "TODO: implement KICK-E2E-010 - optional missing Secret creation triggers kick"
