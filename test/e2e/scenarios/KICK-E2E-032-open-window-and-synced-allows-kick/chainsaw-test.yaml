apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: open-window-and-synced-allows-kick
spec:
  steps:
    - name: scaffold
      try:
        - script:
            content: |
              echo "TODO: implement KICK-E2E-032 - open window and synced allows kick"
