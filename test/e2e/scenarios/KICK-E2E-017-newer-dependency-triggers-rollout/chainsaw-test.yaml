apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: newer-dependency-triggers-rollout
spec:
  steps:
    - name: scaffold
      try:
        - script:
            content: |
              echo "TODO: implement KICK-E2E-017 - newer dependency triggers rollout"
