apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-036
  namespace: kick-e2e-036
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-036
  template:
    metadata:
      labels:
        app: app-036
    spec:
      containers:
        - name: app
          image: registry.k8s.io/pause:3.10
          envFrom:
            - secretRef:
                name: app-secret
