apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-033
  namespace: kick-e2e-033
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-033
  template:
    metadata:
      labels:
        app: app-033
    spec:
      containers:
        - name: app
          image: registry.k8s.io/pause:3.10
          envFrom:
            - secretRef:
                name: app-secret
