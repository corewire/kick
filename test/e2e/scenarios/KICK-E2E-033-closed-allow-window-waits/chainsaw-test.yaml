apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: closed-allow-window-waits
spec:
  steps:
    - name: scaffold
      try:
        - script:
            content: |
              echo "TODO: implement KICK-E2E-033 - closed allow window waits"
