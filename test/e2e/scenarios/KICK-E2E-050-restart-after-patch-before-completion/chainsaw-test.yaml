apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: restart-after-patch-before-completion
spec:
  steps:
    - name: scaffold
      try:
        - script:
            content: |
              echo "TODO: implement KICK-E2E-050 - restart after patch before completion"
