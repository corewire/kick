apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: fallback-finds-one-owner
spec:
  steps:
    - name: scaffold
      try:
        - script:
            content: |
              echo "TODO: implement KICK-E2E-027 - fallback finds one owner"
