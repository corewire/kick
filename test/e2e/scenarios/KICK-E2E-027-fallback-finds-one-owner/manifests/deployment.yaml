apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-027
  namespace: kick-e2e-027
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-027
  template:
    metadata:
      labels:
        app: app-027
    spec:
      containers:
        - name: app
          image: registry.k8s.io/pause:3.10
          envFrom:
            - secretRef:
                name: app-secret
