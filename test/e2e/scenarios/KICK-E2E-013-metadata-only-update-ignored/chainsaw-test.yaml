apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: metadata-only-update-ignored
spec:
  steps:
    - name: scaffold
      try:
        - script:
            content: |
              echo "TODO: implement KICK-E2E-013 - metadata-only update ignored"
