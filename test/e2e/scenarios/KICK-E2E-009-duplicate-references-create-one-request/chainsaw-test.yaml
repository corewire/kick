apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: duplicate-references-create-one-request
spec:
  steps:
    - name: scaffold
      try:
        - script:
            content: |
              echo "TODO: implement KICK-E2E-009 - duplicate references create one request"
