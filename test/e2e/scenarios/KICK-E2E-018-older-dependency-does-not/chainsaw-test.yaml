apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: older-dependency-does-not
spec:
  steps:
    - name: scaffold
      try:
        - script:
            content: |
              echo "TODO: implement KICK-E2E-018 - older dependency does not"
