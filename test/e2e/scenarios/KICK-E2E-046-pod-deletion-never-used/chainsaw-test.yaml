apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: pod-deletion-never-used
spec:
  steps:
    - name: scaffold
      try:
        - script:
            content: |
              echo "TODO: implement KICK-E2E-046 - Pod deletion never used"
