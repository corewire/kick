#!/usr/bin/env bash
set -euo pipefail

if [[ "${SKIP_KICK_PRECOMMIT:-0}" == "1" ]]; then
  exit 0
fi

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

echo "[pre-commit] Running docs-gen-check"
make pre-commit-check
